# バイナリ関係の用語

## `rbp`

関数の基準点になるレジスタ。
ローカル変数の指定などは`rbp`を基準にして行う。

## `rax`

計算用レジスタ。

## `mov`

値をコピーする命令。
`mov A, B`は`A = B`。

## `r64`

レジスタ（64bit）の型。

## `r/m64`

レジスタorメモリ（64bit）の型。

## `disp8`

オフセット。
符号付き8bit整数として扱われる。
